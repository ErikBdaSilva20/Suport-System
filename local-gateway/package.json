{
  "name": "helpdesk-local-gateway",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Simulação local do tenant-gateway (auth mockado + /data/:table sobre Postgres) para desenvolver o template helpdesk-crud sem depender do gateway real.",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "pg": "^8.22.0"
  }
}
