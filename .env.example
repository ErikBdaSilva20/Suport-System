# Copie para .env.local para rodar o app (npm run dev) contra o docker-compose
# (Postgres + local-gateway simulando o tenant-gateway/Neon).
VITE_GATEWAY_URL=http://localhost:8787
VITE_MOCK_MODE=true
